---
title: insightd
description: Self-hosted server awareness for homelabbers. Know what's happening across your servers — without the complexity of Grafana.
template: splash
hero:
  tagline: Self-hosted server awareness for homelabbers. Monitor containers, hosts, and endpoints — without the complexity of Prometheus + Grafana.
  actions:
    - text: Get Started
      link: /guides/quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/goldenproductions/insightd
      icon: external
---

## Install

insightd runs as three containers — Mosquitto, the hub, and an agent — managed with Docker Compose. To monitor a second server you run one more agent container on it. That's the only difference.

```bash
mkdir -p ~/insightd && cd ~/insightd
# create mosquitto.conf + docker-compose.yml (see Quick Start)
docker compose up -d
```

Then open `http://your-server:3000` and follow the setup wizard.

**[Full install guide →](/guides/quick-start/)**

For Kubernetes or k3s clusters, see the [Kubernetes guide](/guides/kubernetes/) — the agent runs as a DaemonSet and reports to the same hub.

## Architecture

```
[agent per host] ─┐
[agent per host] ─┼─► MQTT (Mosquitto) ─► [hub] ─► SQLite ─► Web UI + Email + Webhooks
[agent per host] ─┘
```

Once the hub is running, every feature — email, alerts, webhooks, AI diagnosis, retention, the public status page — is configured from the **Settings** page in the UI. No `.env` file required.

## Free & Open Source

insightd is MIT licensed and free for any number of servers. No cloud dependency, no telemetry, no signup. Your data stays on your hardware.
